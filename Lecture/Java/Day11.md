## Java 11강
#### 객체 : 세상에 존재하는 모든 것
#### 객체는 프로그래밍에서 속성과 기능을 가지는 프로그램 단위
- 사람 : 속성은 키, 몸무게 등, 기능은 의사, 검사, 경찰 등
- 날씨 프로그램 : 속성은 온도, 미세먼지 등, 기능은 날씨 예보 등
- 사칙연산 프로그램 : 속성은 +,-,* 등, 기능은 연산기능
#### 클래스 : 객체를 생성하기 위한 틀, 모든 객체는 클래스로부터 생성된다.
#### 클래스는 속성(멤버 변수)와 기능(메서드)로 구성된다.